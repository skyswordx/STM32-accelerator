# 滤波器学习与模仿输出功能

## 功能概述

本功能实现了一个两阶段的滤波器学习和模仿系统：

1. **学习模式**：通过 DDS 扫频对未知滤波器进行测量，学习其传递函数
2. **模仿输出模式**：对输入信号应用学习到的传递函数，生成对应的输出信号

## 使用方法

### 第一步：连接硬件

- 将 DDS 输出连接到未知滤波器的输入端
- 将滤波器输出连接到 ADC2 输入端
- 将 DDS 输出同时连接到 ADC1 输入端（作为参考信号）

### 第二步：学习模式

1. 按下用户按钮（PC1）启动学习模式
2. 系统自动进行频率扫描（1kHz - 50kHz，步长 100Hz）
3. 对每个频率点进行 FFT 分析，计算传递函数
4. 完成后自动识别滤波器类型并计算传递函数参数
5. 学习完成后，系统提示可以进入模仿模式

### 第三步：模仿输出模式

1. 将信号源连接到 ADC1 输入端
2. 将 DAC1 输出连接到目标设备
3. 再次按下用户按钮（PC1）进入模仿模式
4. 系统每 20ms 自动采样输入信号
5. 通过 FFT、传递函数计算、IFFT 生成输出信号
6. 输出信号通过 DAC1 输出

### 第四步：停止功能

- 再次按下用户按钮（PC1）停止当前模式，返回空闲状态

## 技术参数

- **扫频范围**：1kHz - 50kHz
- **频率步长**：100Hz
- **采样模式**：双 ADC 同步采样
- **FFT 长度**：4096 点
- **更新周期**：20ms（模仿模式）
- **支持滤波器类型**：低通、高通、带通、带阻

## 文件结构

```
Module/
├── ADC/
│   ├── my_adc_task.c/h     # ADC任务，包含学习和模仿模式
├── DAC/
│   ├── my_dac_task.c/h     # DAC任务，软件DDS输出
├── Parser/
│   ├── filter_identification.c/h  # 滤波器识别算法
│   ├── filter_imitate.c/h         # 滤波器模仿算法
└── Param/
    ├── my_parameter_config.c/h    # 全局参数配置
```

## 注意事项

1. **定时器配置**：需要在 CubeMX 中配置 TIM6 为 20ms 周期定时器（如需要自动触发）
   - 详细配置步骤请参考：`TIM6配置指南.md`
2. **内存对齐**：所有 DMA 缓冲区都已进行 32 字节对齐
3. **缓存一致性**：所有 DMA 操作后都进行了 D-Cache 清理
4. **数值稳定性**：所有除法运算都添加了零检查
5. **频率限制**：传递函数计算在 Nyquist 频率附近可能不够准确

## 调试输出

系统通过串口输出详细的调试信息：

- 扫频进度和每个频率点的测量结果
- 传递函数识别结果
- 模仿模式的处理状态
- 错误信息和警告

## 扩展功能

- 可通过修改`SWEEP_F_START_HZ`、`SWEEP_F_STOP_HZ`、`SWEEP_F_STEP_HZ`调整扫频参数
- 可通过修改`FFT_LENGTH`调整频域分辨率
- 可添加更多滤波器类型支持
- 可优化传递函数参数以提高精度
